<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full h-screen text-white p-10 bg-zinc-900">
        <div class="nav mb-5">
            <a href="/" class="text-zinc-400" >~ Create Users</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length > 0) { %>
                <% users.forEach((user) => { %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-md">
                        <div class="w-full h-52 rounded-md bg-gray-600 overflow-hidden">
                            <img src="<%= user.image %>" alt="Image" class="w-full h-full object-cover rounded-md">
                        </div>
                        <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                        <h5 class="text-zinc-500"><%= user.email %></h5>
                        <div class="flex gap-3">
                            <a class="text-blue-600" href="/edit/<%= user._id %>">Edit User</a>
                            <a class="text-red-600" href="/delete/<%= user._id %>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="w-[15rem] px-3 py-2 mt-5 bg-zinc-800 rounded-md border-2 border-zinc-800">
                    <h2 class="text-xl text-red-500">No Users Till Now.....!!</h2>
                </div>
            <% } %>
        </div>
    </div>
</body>

</html>